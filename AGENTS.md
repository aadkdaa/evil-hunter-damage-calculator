# 작업 지침

이 프로젝트는 이블헌터타이쿤의 데미지 공식을 웹 계산기로 구현하는 프로젝트입니다.

## 작업 시작 시 확인

1. `docs/현재상태.md`에서 구현 완료 범위와 다음 작업을 확인합니다.
2. `docs/개발규칙.md`의 화면·계산·저장 규칙을 따릅니다.
3. 공식이나 수치 근거가 필요한 작업은 `docs/참고자료.md`를 통해 원본 문서를 확인합니다.
4. 기존 사용자 변경을 보존하기 위해 `git status`와 `git diff`를 먼저 확인합니다.

## 핵심 지침

- 사용자의 최신 요청이 문서보다 우선합니다. 요청으로 기준이 바뀌면 관련 문서도 함께 갱신합니다.
- `기본세팅`과 `데미지 계산 2`를 최종 사용 방식과 화면 디자인의 기준으로 사용합니다.
- 그 밖의 기존 탭은 엑셀 내용을 우선 옮긴 임시 구현으로 취급하며 최종 UI 기준으로 사용하지 않습니다.
- 확정 공식과 미확정 가설을 코드와 화면에서 섞지 않습니다.
- 원본 엑셀·이미지·텍스트 파일은 수정하지 않습니다. 공식 조사 문서는 확정 정보가 추가될 때만 해당 분류 문서와 조사 히스토리에 기록합니다.
- 사용자가 요청하기 전에는 GitHub Pages 배포나 외부 공개를 진행하지 않습니다.

## 문서 역할

- `docs/개발규칙.md`: 자주 바뀌지 않는 구현 및 디자인 규칙
- `docs/현재상태.md`: 완료 기능, 보류 기능, 다음 작업과 세션 인계 상태
- `docs/참고자료.md`: 원본 자료와 공식 문서의 위치 및 용도
- 코드 변경 이력: Git 커밋으로 관리하며 별도 구현 히스토리 문서를 만들지 않습니다.
- 공식 조사 이력: `C:\Users\Public\Shared\DamageCalculator\docs\히스토리.md`에서 관리합니다.

## 기록 위치 판단

- 계속 적용할 개발 방식이 바뀌면 `docs/개발규칙.md`를 수정합니다.
- 구현 완료 범위나 다음 작업이 바뀌면 `docs/현재상태.md`를 수정합니다.
- 참고할 파일이나 문서의 위치·용도가 바뀌면 `docs/참고자료.md`를 수정합니다.
- 공식, 검산 결과, 게임 정보가 새로 확정되면 `DamageCalculator`의 주제별 문서와 `docs/히스토리.md`를 수정합니다.
